#!/bin/sh

INSTALL_PREFIX=@INSTALL_PREFIX@

for f in crtn.3.gz
do
  FILE=${INSTALL_PREFIX}/share/man/man3/${f}
  chmod 444  ${FILE}
  chown root ${FILE}
  chgrp root ${FILE}
done

FILE=${INSTALL_PREFIX}/lib/libcrtn.so
chmod 444  ${FILE}
chown root ${FILE}
chgrp root ${FILE}


